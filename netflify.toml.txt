[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"
```
4. Save with **Ctrl + S**
---
Then in command prompt:
```
cd C:\\Users\\piyus\\my-app
git add .
git commit -m "add netlify config"
git push